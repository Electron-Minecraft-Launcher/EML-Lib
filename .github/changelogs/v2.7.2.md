## Changelog

### Changes

- Removed the `pngjs` dependency from the project; using custom PNG parsing logic instead, so the `Skin` class could be used in both Node.js and browser environments without requiring additional dependencies.

### Bug fixes and improvements

- Updated JSDoc and README documentation to reflect the removal of `pngjs` and to provide clearer information about requirements.
- Updated dependencies to the latest versions.
